import 'package:flutter/material.dart';

const double maxCategoryCardSize = 400;

class CategoryCard extends StatelessWidget {
  const CategoryCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
